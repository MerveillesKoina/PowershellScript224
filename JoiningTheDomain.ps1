########################################### # This script is for the DM and SWS VMs# This script has commands that will join#  the DM server and SWS client to the#  domain called mydomain.local############################################ AFTER YOU RUN "CreatingTheDomain" script on DC,# ^^^^^^^^^# RUN THIS SCRIPT ONLY ON YOUR # 1) Domain Member Server VM (DM) AND the# 2) Win10 VM (SWS) ## Change directory to root directory C:\#cd \## NOTE: Remember your domain name: "mydomain.local".# (A) Create a variable for the domain name$domainName = "mydomain.local"## (B) To join the domain, you need domain administrator creds# Create a credential object variable to store username and password:    #$username  = "mydomain\Administrator" $password  = ConvertTo-SecureString -String "Password01" -AsPlainText -Force$joinCreds = [PSCredential]::New($username, $password)## (C) Add the two machines to the Domain using creds above ($joinCreds)#     On your DM1 VM, open ISE as Administrator. Enter the following#     lineAdd-Computer -DomainName $domainName -Credential $joinCreds## (D) Reboot to make the machine join the domain#Restart-Computer#######################################################(E) On the SWS VM, open ISE as Administrator, and repeat (A) to (D)to join that VM also to the domain#######################################################